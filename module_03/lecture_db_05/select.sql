-- Select all columns from the USERS table
-- SELECT * FROM USERS;

-- Select only the NAME and LOCATION columns from the USERS table
-- SELECT NAME, LOCATION FROM USERS;

-- Select NAME as LOCAL_NAME and LOCATION from the USERS table
-- SELECT NAME as LOCAL_NAME, LOCATION FROM USERS;
